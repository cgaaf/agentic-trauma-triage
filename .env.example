ANTHROPIC_API_KEY=insert-anthropic-api-key
DEEPGRAM_API_KEY=insert-deepgram-api-key

# Rate limiter (required)
RATE_LIMIT=10
RATE_LIMIT_WINDOW_MS=60000

STORYBOOK_API_TARGET=insert-dev-api-host

# Optional: allow a single extra origin for CSRF (preview deploys, custom domains).
# Only one origin is supported — comma-separated lists are NOT parsed.
# ALLOWED_ORIGIN=https://preview.example.com

# Max voice recording duration in seconds (default: 30)
PUBLIC_MAX_RECORDING_DURATION_S=30

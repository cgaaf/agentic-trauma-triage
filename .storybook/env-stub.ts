// Stub for $env/dynamic/public — SvelteKit virtual module not available in Storybook.
// All PUBLIC_ env vars will be undefined, so consumers must provide defaults.
export const env: Record<string, string | undefined> = {};
